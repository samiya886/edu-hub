{
  "name": "edu-hub",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm install --prefix Backend1 && npm install --prefix client && npm run build --prefix client",
    "start": "npm start --prefix Backend1"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
